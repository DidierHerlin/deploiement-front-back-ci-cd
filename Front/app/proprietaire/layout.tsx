import { ProprietaireLayout } from "@/proprietaire/components/ProprietaireLayout"

export default function Layout({ children }: { children: React.ReactNode }) {
  return <ProprietaireLayout>{children}</ProprietaireLayout>
}
